<h1 class="text-center">Calendar</h1>
<%= month_calendar events: @events do |date, events| %>
  <%= date.strftime("%-m/%-d") %>

  <% events.each do |event| %>
    <div>
      <p><%= event.name %></p>
    </div>
  <% end %>
<% end %>
